# Firestore Rules Fix

The Firestore rules have been updated to properly secure paths and allow RAG document reads.

## Deployment Instructions

To deploy these rules, simply use the AI agent's "deploy_firebase" tool, or manually copy the contents of `firestore.rules` into your Firebase Console.
