# Implementation status — current repair

The full 300-point feature request is preserved in `REQUESTED_300_FEATURES.txt`.
This repair release does **not** claim that all 300 roadmap items were newly
implemented. It completes the high-impact defects in the current request:
Google authentication, sign-in UI, exact-question answer replacement,
question-number mismatch rejection, reasoning/source-label cleanup, duplicate
answer prevention, and Sinhala Unicode shaping.

The remaining roadmap items should be delivered in staged releases with
acceptance tests instead of one unverified bulk change.
